[project]
name = "derja-ai"
version = "0.1.0"
description = "Derja Ai mobile chat with AgentRouter Opus 5"
requires-python = ">=3.10"
dependencies = [
    "httpx[http2]>=0.27.0",
    "h2>=4.0.0",
]

[tool.vercel]
entrypoint = "api.main:handler"
